AI 小镇 Web 游戏

目标
借助 AI 辅助开发工具，从 0 到 1 完成一个可运行、可体验的 Web 版 AI 小镇 MVP。
小镇里有若干名 NPC，每人有自己的属性和性格，能根据当前状态自主决定下一步行动。玩家可以观察小镇、查看 NPC、与 NPC 对话，并可影响世界状态。
不要求复刻斯坦福小镇，不要求复杂系统。
重点：你能否把需求拆清楚、把系统做出来、把 AI 真正用起来，并讲明白你做了什么、为什么这么做。
要求
●能本地启动并演示，最好能提供可访问地址；
●至少 2 个地点、2 名性格/属性有明显差异的 NPC；
●玩家可查看 NPC 信息，并与其对话；
●NPC 不靠写死的脚本循环，能依据自身状态决定下一步行动；
●前端通过后台 API 与数据交互，后台可保存/恢复世界状态；
●至少接入一次真实 AI 调用（用于决策或对话），未配置密钥时有可体验的降级方案；
●一份 README，说明怎么跑、用了什么技术、AI 怎么接入、你本人做了哪些关键决策。
技术栈、架构、接口设计、数据结构、NPC 人设与地图，全部由你决定，并自行在文档中说明取舍。
评估
1.能否按 README 启动并跑通主流程；
2.NPC 是否真的「自主决策」（而非固定动画 / 固定文案）；
3.不同人设的 NPC，其行为或对话是否有可辨识的差异；
4.AI 调用失败或未配置密钥时，能否降级到可体验模式；
5.候选人能否解释关键模块，并现场改一段代码；
6.工程基本素养：配置管理（密钥不入库）、错误处理、至少一处自动化测试。
交付物
●源代码（前端 + 后台 + 初始化数据 + .env.example）；
●技术方案文档（玩法、技术选型、接口与决策设计、AI / Mock 模式、你本人的关键决策与一处 AI 修改案例；可独立成文档，也可作为 README 专章）；
●可访问的线上体验地址（公开可访问的 Web 地址；若资源受限无法长期开放，需注明有效期及替代演示方式）；
●视频录制讲解（边操作边讲解，覆盖查看小镇、NPC 行动、对话及 AI 降级，3–5 分钟为宜）。

---

# 基础架构与设计（技术方案草案）

> 以下为在原始要求之上补充的技术选型与设计文档。参考了 2025 年港科大团队的多智能体沙盒作品 [Aivilization](https://hkust.edu.hk/news/hkust-launches-worlds-largest-ai-powered-educational-sandbox-game-advancing-ai-literacy-and)（10 万 AI agent 的 MMO 式社会模拟），取其可落地于 MVP 的设计思想，不复刻其规模。

## 0. 从 Aivilization 借鉴什么

| Aivilization 的做法 | 本项目的裁剪落地 |
|---|---|
| 三层架构：社会层 / 个体层 / 认知层 | 简化为两层：**世界模拟层**（tick 循环、地点、时间）+ **NPC 认知层**（状态 → 决策 → 行动 → 记忆） |
| 日记式记忆：短期事件 + 长期信念/情绪 | 短期记忆 = 最近 N 条事件环形缓冲；长期记忆 = LLM 定期摘要成「日记」 |
| MBTI 人格影响行为偏好 | 每个 NPC 有 personality 字段（人设 prompt + 数值化偏好权重），驱动可辨识的行为/对话差异 |
| 「游戏模式 / 数据模式」双视图 | 前端提供像素地图视图 + NPC 详情面板（属性、当前动作、记忆日志可视化） |
| 每 agent 每月 $2 的成本控制 | 用小模型 + 结构化 JSON 输出 + 低频决策（仅在 tick 到点或玩家交互时调用），无密钥时降级为规则引擎 |

## 1. 技术栈总览

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | **React 18 + TypeScript + Vite** | 生态成熟、AI 辅助开发资料最全、构建快 |
| 游戏渲染 | **Phaser 3**（嵌入 React 页面） | 自带 Tilemap/精灵/动画/相机，2D 像素小镇开箱即用 |
| 美术表现 | **2D 像素风**：免费素材（Kenney / itch.io LPC 角色集）+ Tiled 编辑地图 | 与斯坦福小镇/Aivilization 同一视觉语言，无需原创美术，NPC 头顶气泡显示当前动作 |
| 后端 | **Python 3.12 + FastAPI** | AI 生态最强（SDK/提示工程/结构化输出），异步支持 tick 循环，Pydantic 天然做接口校验 |
| 实时通道 | REST（指令/查询）+ **SSE**（世界事件推送） | 单向推送足够，比 WebSocket 简单，Nginx 反代零配置 |
| AI 接入 | **OpenAI 兼容抽象层**：默认 DeepSeek-V4 系列，`.env` 可换任意兼容端点 | 一套代码适配多家；决策用结构化 JSON 输出，对话用流式 |
| 降级方案 | **规则引擎（效用驱动）**：无密钥/调用失败时自动切换 | 取当前最低需求值（饿/困/社交）决定行动；对话走人设模板库，保证可体验 |
| 数据库 | **SQLite + SQLModel** | 单文件即全部世界状态，保存/恢复 = 文件快照，零运维；MVP 规模远够 |
| 部署 | **Docker Compose**（backend + Caddy）部署到自有服务器 | Caddy 自动 HTTPS + 托管前端静态产物 + 反代 API/SSE，一条 `docker compose up -d` 上线 |
| 测试 | pytest（决策引擎、降级切换、API 冒烟）+ 前端 Vitest 可选 | 满足「至少一处自动化测试」并覆盖核心风险点 |

## 2. 系统架构

```
┌─ 浏览器 ──────────────────────────────┐
│ React 壳层（NPC 面板 / 对话窗 / 上帝操作）│
│ Phaser 3 场景（Tilemap + NPC 精灵）      │
└──────── REST ↓ ↑ SSE ────────────────┘
┌─ FastAPI ────────────────────────────┐
│ API 层: /world /npcs /chat /actions   │
│ 模拟层: Tick 调度器（如 30s/tick）      │
│ 认知层: NPC 决策器 ──→ AI Provider 抽象 │
│                        ├─ LLM 模式    │
│                        └─ Mock 规则引擎│
│ 持久层: SQLModel → SQLite（可快照恢复） │
└──────────────────────────────────────┘
```

- **Tick 循环**：世界每 tick 推进一次；每个 NPC 在自己的决策间隔到期时被唤醒，组装上下文（人设 + 当前状态 + 短期记忆 + 长期日记摘要 + 可用行动列表）→ 请求决策 → 校验 JSON → 执行（移动/工作/休息/找人聊天）→ 写入记忆 → SSE 广播。
- **玩家影响世界**：提供 2–3 个「上帝操作」（如切换天气、发布小镇公告、给 NPC 送物品），写入世界状态并进入 NPC 下一次决策上下文，形成可演示的因果链。

## 3. NPC 数据结构（核心设计）

```jsonc
{
  "id": "npc_ling",
  "profile": {                     // 静态人设（初始化数据写死）
    "name": "小玲", "role": "咖啡店店主",
    "personality": "ENFP，热情外向，爱打听八卦",
    "backstory": "……",
    "weights": { "social": 1.4, "work": 1.0, "rest": 0.7 }  // 降级规则引擎用
  },
  "state": {                       // 动态状态（每 tick 演化）
    "location": "cafe",
    "action": { "type": "chat", "target": "npc_bo", "reason": "…" },
    "needs": { "energy": 62, "hunger": 40, "social": 85 },
    "mood": "cheerful"
  },
  "memory": {
    "short_term": [ "…最近 20 条事件…" ],   // 环形缓冲
    "diary": [ "第1天：……" ]               // LLM 定期摘要，超长截断
  },
  "relationships": { "npc_bo": { "affinity": 30, "impression": "话少但可靠" } }
}
```

**决策接口（LLM 结构化输出）**：`{ "action": "move|work|rest|eat|chat|idle", "target": "...", "say": "...", "reason": "..." }` —— `reason` 字段既用于头顶气泡展示「自主决策」，也用于答辩时解释 AI 行为。

**初始 NPC 与地点**：见 `outline.md`（Inconnewt 世界观：劫后小镇,莫莫/小柯/阿羯/利利 四位 NPC,人设卡直接映射到 persona prompt 与降级规则引擎 weights;prompt 按「共享世界观 + 每角色私有人设 + 输出格式」三层拆分）。

## 4. 接口设计（REST + SSE）

| 接口 | 说明 |
|---|---|
| `GET /api/world` | 世界快照（时间、天气、地点、全部 NPC 状态） |
| `GET /api/npcs/{id}` | NPC 详情（人设/状态/记忆/关系） |
| `POST /api/chat/{id}` | 玩家与 NPC 对话（流式返回，写入 NPC 记忆） |
| `POST /api/world/actions` | 上帝操作（天气/公告/送物品） |
| `GET /api/events` (SSE) | 推送 NPC 行动、位置变化、世界事件 |
| `POST /api/world/save` · `/load` | 世界状态保存 / 恢复 |

## 5. 工程约束

- 密钥仅走 `.env`（提供 `.env.example`），`AI_PROVIDER=mock|openai_compatible` 一键切换；
- AI 调用统一超时 + 重试一次 + 失败自动落规则引擎，前端标注当前模式（真实 AI / Mock）；
- pytest 覆盖：决策 JSON 校验、mock 决策的确定性、保存/恢复往返一致性。

---

# 基础设计 Todo List

> 状态更新（2026-08-25）：仅对已经落地并验证的完整事项打勾；部分完成项保留未勾选，并在行末注明当前进度。

## M0 — 项目脚手架
- [ ] 初始化 monorepo：`frontend/`（Vite + React + TS + Phaser）、`backend/`（FastAPI + SQLModel）、`docker-compose.yml`（React、Phaser、FastAPI、SQLite 与 Compose 已完成；持久层使用轻量 sqlite3 而非 SQLModel）
- [x] `.env.example`（AI_PROVIDER、API_KEY、BASE_URL、MODEL、TICK_SECONDS）
- [x] git 初始化 + `.gitignore`（密钥/数据库文件不入库）

## M1 — 世界模拟层（先跑 Mock）
- [x] 定义数据模型：World / Location / NPC(profile, state, memory, relationships)
- [x] 初始化数据：按 `outline.md` 落地——5 个地点 + 莫莫、利利、小柯、阿羯 4 位 NPC
- [x] Tick 调度器 + 效用规则引擎（无 AI 也能自主行动）
- [x] SQLite 持久化 + save/load 接口

## M2 — AI 认知层
- [x] AI Provider 抽象（openai_compatible / mock 双实现）
- [x] 决策 prompt：人设 + 状态 + 记忆 → 结构化 JSON 行动；校验失败落规则引擎
- [x] 对话 prompt：人设 + 关系 + 记忆 → 流式回复；对话写入短期记忆
- [x] 记忆摘要任务（短期 → 日记）
- [x] 超时/重试/降级链路 + 模式标识

## M3 — 前端小镇
- [ ] Tiled 制作小镇地图（v2 已按备选方案落地 Phaser 手绘底图、路径点和 NPC 移动；Tiled 素材管线留作后续美术迭代）
- [x] SSE 接入：NPC 行动/位置实时同步，头顶气泡显示当前动作与 reason
- [x] NPC 详情面板（人设/需求条/记忆日志）——「数据模式」视图
- [x] 对话窗口（流式渲染）+ 上帝操作栏（天气/公告/送物品）

## M4 — 工程收尾
- [x] pytest：决策校验 / mock 确定性 / 保存恢复往返（现有 8 项核心测试）
- [x] README + 技术方案文档（含关键决策与一处 AI 修改案例）
- [ ] Docker Compose + Caddy 部署到自有服务器，验证公网可访问（Compose 部署已验证；仓库不记录私有部署地址）
- [ ] 录制 3–5 分钟演示视频（小镇总览 → NPC 自主行动 → 对话 → 拔掉密钥演示降级）

---

# 迭代方向 v2 —— 从「观察仪表盘」到「游戏」（新增需求）

## 问题诊断（基于当前 demo）

1. **不像游戏,像后台管理页**:地图是静态信息卡片,NPC 是不动的图标,没有场景、没有角色实体、没有点击反馈的仪式感;前端实际未引入 Phaser(v1 方案未落地)。
2. **对话是聊天表单,不是剧情演出**:缺少立绘/角色感/演出节奏,与 NPC 说话像在用客服窗口。
3. **自主决策显得单薄**:NPC 连续多个 tick 重复同一动作与同一句 reason(见莫莫记忆:连续 4 小时「货架整理完了」),行为没有日程感、没有跨 NPC 互动,「自主」不可信。

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在迭代 Inconnewt(劫后小镇 AI 模拟游戏)。保持后端架构(FastAPI + tick + SSE + AI Provider 抽象)与 outline.md 世界观不变,完成以下三组改造。视觉上**不照抄明日方舟的黑色科技风**,保留现有「纸质档案 × 橄榄绿 × 劫后暖调」配色,但借鉴其**游戏 HUD 的交互骨架**。

### A. 游戏化呈现层(舞台)

- 引入 **Phaser 3**:全屏 2D 小镇场景作为页面主体(不再是卡片网格)。用 Tilemap(免费像素素材,按 outline.md §6 色板调色)铺出 4 个地点 + 连接道路;做不出 tilemap 就先用手绘风单张大底图 + 坐标锚点,但 NPC 必须是**场景里会走动的精灵**。
- NPC 精灵有**寻路移动**(A* 或路径点即可)、行走动画、头顶昵称与当前动作气泡;移动过程可见,而不是瞬移。
- **昼夜与天气可视化**:根据世界时间给场景叠加色调(白天/黄昏/夜晚压蓝),雾天加雾效;夜晚广场篝火有光晕。世界时间流逝在场景中「看得见」。
- 保留现有仪表盘 UI 作为可切换的「数据模式」(Aivilization 双视图),默认进入「游戏模式」。

### B. 交互设计(借鉴明日方舟的 HUD 骨架)

- **全屏场景为底,UI 悬浮四角**:左上世界状态(天数/时刻/天气),右上系统按钮(保存/恢复/数据模式/AI 模式标识),底部为「上帝操作」快捷栏,中央永远是小镇舞台。去掉页面滚动,整个游戏一屏呈现。
- **点击 NPC → 侧滑角色档案面板**(参考干员档案):大字号名字 + 代号式头衔、性格标签 chips、需求条、分页 Tab(档案/记忆/关系)。为每人生成一张**立绘**(可先用大尺寸像素半身像或 AI 生成插画占位,风格统一)。
- **对话改为 AVG 剧情式演出**:进入对话后场景压暗,底部对话框 + 角色立绘在侧,NPC 回复用**打字机逐字效果**(配合流式输出),玩家输入框在对话框下方。对话中 NPC 的 mood 变化可反映在立绘表情或对话框描边色上。
- **反馈仪式感**:按钮 hover/点击有状态变化;上帝操作触发后,场景内有对应演出(起雾=雾效渐入,公告=广场公告板闪烁)+ 事件横幅提示;NPC 决策更新时气泡有淡入动画。
- 可选加分:开场标题画面(Inconnewt logo + 蝾螈徽记 + 「进入小镇」),循环环境音/BGM 开关。

### C. 自主决策深化(让「自主」可信)

- **日程计划层(plan-then-act 轻量版)**:每个游戏日开始时,让 LLM 为每个 NPC 依据人设+昨日日记生成 3–5 条当日计划(如「上午整理货架,傍晚去水潭边坐坐」);每 tick 决策时把当日计划 + 已完成项放入上下文,行动应大体沿计划推进、允许因事件偏离。
- **反重复约束**:决策上下文中加入「最近 3 次行动」,prompt 明确要求:连续重复同一动作必须给出新理由,否则必须换行动;后端校验若与上次完全相同(动作+理由),强制注入「你已经做这件事很久了」提示重新决策一次。
- **扩大动作空间**:在 move/work/rest/eat/chat/idle 之上补充地点特有动作(莫莫「擦拭旧物」、利利「照料植物」、阿羯「登塔瞭望」、小柯「拆装零件」)与 `visit`(去别人的地点串门),动作定义放数据不放代码。
- **NPC 间自主互动**:两个 NPC 处于同一地点时,按人设与关系概率触发一段 2–4 轮的 AI 简短对话,写入双方记忆、影响 affinity,并作为「小镇动态」推送到事件流——这是演示「涌现」的核心证据。
- **记忆写入去重**:相同内容不重复入库;短期记忆满时触发 LLM 摘要进日记,日记再反哺次日计划,形成「行动→记忆→计划」闭环。
- **事件流叙事化**:底部/侧边「镇上刚刚发生」时间线改为叙事句式(「黄昏,利利端着热汤去修理棚找小柯」),Mock 模式下由模板拼接,同样保持人设语气。

### 验收标准(v2)

1. 打开页面第一眼是「一个小镇在过日子」:角色在场景里走动、天色在变,而非信息面板;
2. 观察 10 个 tick,单个 NPC 至少出现 3 种不同行动,且能看到一次 NPC 间自主互动;
3. 对话体验有剧情演出感(压暗+立绘+打字机),而非聊天窗;
4. 所有 v1 既有能力(SSE、保存恢复、AI/Mock 降级、测试)不回退,Mock 模式下 A/B/C 均可体验。

## v2 Todo

- [x] 前端引入 Phaser 3,搭全屏场景 + 手绘大底图 + NPC 精灵路径点移动与动画
- [x] 昼夜色调 / 天气特效 / 篝火光晕
- [x] HUD 重构:四角悬浮布局 + 数据模式切换
- [x] 角色档案侧滑面板(立绘、标签、分页)
- [x] AVG 式对话演出(压暗、立绘、打字机流式)
- [x] 上帝操作的场景内演出反馈
- [x] 后端:每日计划生成 + 计划入决策上下文
- [x] 后端:反重复约束 + 扩展动作表(数据驱动)+ visit 串门
- [x] 后端:同地点 NPC 自主对话(2–4 轮,写记忆改好感,进事件流)
- [x] 记忆去重 + 摘要闭环;事件流叙事化(AI/Mock 双模板)
- [x] 新增 pytest:反重复决策、每日计划生成降级、NPC 互动触发

---

# 迭代方向 v2.1 —— 场景美术落地 + 性能修补（新增需求）

## 问题诊断（基于 v2 代码审查）

1. **场景没有图**:`TownStage.tsx` 里整个小镇是 Phaser Graphics 画的几何色块(建筑=矩形、NPC=程序生成的圆头小人),项目从未加载过任何场景贴图;`public/assets` 只有 4 张档案立绘。
2. **卡顿三个实锤**:
   - **Tween 泄漏(主因,越开越卡)**:`syncWorld` 每次世界更新都新建 night/fog 补间且不清理旧的;`walkActor` 的 `bob` 补间(`repeat:-1`)挂在 sprite 上,打断移动时只 `killTweensOf(container)`,连续移动两次后旧 bob 永远杀不掉。补间数量随运行时间线性增长。
   - **合成器压力**:canvas 上层盖了 `mix-blend-mode: soft-light` 的 `.paper-grain`(迫使浏览器每帧重混整个 WebGL canvas)+ 6 处常驻 `backdrop-filter: blur`。
   - **立绘太重**:~550KB/张的 PNG 直接当背景,再套 20px `drop-shadow` filter,对话时每帧对大图算模糊。

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在给 Inconnewt v2 做美术落地与性能修补。**不改后端、不改交互结构**,只动前端渲染层。分两组,P 组(性能)先做——美术贴图加上去之前必须先止住泄漏,否则贴图只会更卡。

### P. 性能修补（先做,半天量级）

1. **修 tween 泄漏**:
   - `syncWorld` 中对 night/fog 补间前先 `this.tweens.killTweensOf(this.night)` / `killTweensOf(this.fog)`;更好的做法是仅当目标值变化时才补间(记录上次 nightAlpha/fogAlpha,相同则跳过);
   - `walkActor` 打断时同时 `killTweensOf(actor.sprite)` 与 `killTweensOf(actor.bubble)`,并把 sprite 角度归零;
   - 自验:控制台跑 `scene.tweens.getTweens().length`,连续推进 20 个 tick,数量必须稳定不增长。
2. **世界更新去抖**:`setWorld` 入口比较 `tick_index` + 天气 + 公告 + 各 NPC 位置,无实质变化直接 return,避免 SSE 高频触发重复 sync;SSE handler 里的 `api.world()` 拉取加 300ms 节流。
3. **减合成器负担**:`.paper-grain` 去掉 `mix-blend-mode`(改成低透明度普通叠加,或把纸纹搬进 Phaser 作为最顶层 tileSprite);`backdrop-filter: blur` 只保留对话遮罩一处,HUD 面板改为不透明底色(当前深色底 + 0.72 透明度视觉差异极小)。
4. **立绘瘦身**:4 张立绘转 WebP、长边 ≤1000px、单张 ≤150KB;`drop-shadow` filter 换成立绘图内预烘焙阴影或一层简单半透明衬底;进入小镇时预加载,避免首次开对话闪加载。
5. **Phaser 配置**:`type: Phaser.WEBGL` 写死(排除 Canvas 回退导致两个全屏 MULTIPLY 矩形巨慢的情况),加 `powerPreference: "high-performance"`。

### A. 场景美术落地（两条路线二选一,推荐路线 1）

> 共同原则:场景只需要**一张静态底图**,不引入 Tiled 运行时解析。建筑标签、点击热区、NPC 锚点全部沿用现有坐标体系,只是把「色块」换成「贴图」。底图做 2000×1300(逻辑 1000×650 的 @2x),`add.image(500, 325, "town").setDisplaySize(1000, 650)`,顺带解决现在 FIT 拉伸发虚的问题。

- **路线 1(推荐):免费像素素材拼底图**。用 CC0 素材包 Ninja Adventure(pixel-boy.itch.io/ninja-adventure,含完整村庄 tileset + 角色)或 Kenney RPG 系列,在 Tiled 里按现有 5 地点布局拼一张小镇,**导出为整张 PNG** 放入 `public/assets/town.png`,再按 outline.md §6 色板整体调色。
- **路线 2:AI 生成整张底图**。按 outline.md §6 给图像模型出图:「俯视 45° 像素风劫后小镇,橄榄绿+灰褐+暖橙,五个区域(杂物铺/修理棚/瞭望塔/温室/中央广场水潭),位置对应现有坐标布局」,生成后人工核对地点与锚点对齐。风险:出图位置对不上时需要微调锚点。
- **NPC 精灵换装**:用素材包的 4 方向行走 spritesheet(每人 3–4 帧×4 向),按 outline.md 各角色主色改色;`walkActor` 播放对应朝向行走动画,到达后停 idle 帧。程序绘制小人退役。
- **氛围层次序**:底图 → NPC 精灵 → 篝火光晕 → 夜色/雾 → 纸纹。夜色矩形与雾保留现有实现即可。
- **版权注记**:README 素材来源一节列明素材包与许可(CC0/署名)。

### 验收标准（v2.1）

1. 首屏是一张有质感的像素小镇底图,NPC 是有行走动画的像素精灵,无几何色块裸露;
2. 连续推进 20 tick + 开关对话 5 次,`tweens.getTweens().length` 稳定,Chrome Performance 面板无持续增长的帧耗时;
3. 常态 60fps,对话开启瞬间无明显掉帧;
4. 立绘全部 ≤150KB,首次打开对话无加载闪烁。

## v2.1 Todo

- [ ] P1 修 tween 泄漏(syncWorld 去重 + walkActor 全量 kill)并用 getTweens 自验（实现与调试计数入口已完成；当前无可连接浏览器，20 tick 控制台计数待复核）
- [x] P2 世界更新去抖(tick_index 比较 + SSE 节流)
- [x] P3 去 mix-blend-mode / 收敛 backdrop-filter 至 1 处
- [x] P4 立绘转 WebP ≤150KB + 预载 + 去 drop-shadow filter
- [x] P5 Phaser 强制 WEBGL + high-performance
- [x] A1 采用路线 2，生成并接入 2000×1300 原创小镇底图
- [x] A2 NPC 换 4 向 × 3 帧行走 spritesheet + 朝向动画
- [x] A3 氛围层重排(底图/精灵/光晕/夜雾/纸纹)+ README 素材许可注记

---

# 迭代方向 v3 —— 可玩性:玩家成为小镇一员（新增需求）

## 问题诊断（基于 v2.1 体验）

1. **玩家没有身份**:玩家是悬浮在世界外的「上帝」,点面板、按按钮,与小镇没有关系,交互(对话/天气/送礼)改变了 NPC 状态但玩家感知不到后果,所以「不好玩」。
2. **交互没有回报回路**:对话完了就完了——没有好感可视化、没有目标、没有可解锁的东西,玩家缺少「再玩一会儿」的理由。
3. **场景地点偏少**:5 个地点一屏放完,没有探索感。

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在给 Inconnewt 做 v3「可玩性」迭代。核心翻转:**玩家不再是上帝,而是「劫后十年,第一个走进新螈镇的外来者」**——这个身份直接长在 outline.md 世界观上(阿羯对陌生人警惕、利利见谁都先递热汤、莫莫想打听旧世界、小柯对外面的零件好奇),四人对玩家的初始态度天然差异化。保留既有架构(tick/SSE/AI Provider/降级),原始要求的所有能力不回退;原「上帝操作」降级为右上角「观察者模式」里的调试栏(演示降级与快速改天气仍用它)。

### A. 玩家化身与探索

- **玩家精灵**:同素材包风格的角色(可选 2–3 个外观),出生点为新增地点「旧巴士站」(玩家来小镇的方式,写进世界观)。
- **移动**:点击地面寻路 + WASD 双支持;摄像机跟随玩家(lerp 平滑),地图扩到 1600×1050,一屏只看到局部——探索感来自「走过去看」。
- **新增 2 个地点**(合计 7 个):`旧巴士站`(出生点/存档点)与`废墟界碑`(镇子与废墟的边界,阿羯剧情与悬念暗线的落点;夜里有微光)。【地点人设遵循 outline.md,新增设定同步补进 outline.md】
- 玩家状态入世界模型:`player` 也有 location,持久化在存档里。

### B. 邻近交互(对话变得「有身体」)

- **靠近才能对话**:与 NPC 距离小于阈值时其头顶亮出交互提示,按 E 或点击进入 AVG 对话;远处 NPC 只能观察不能交谈。
- **NPC 感知在场情境**:对话 prompt 注入「当前地点/时刻/天气/还有谁在场」,NPC 的回答要引用情境(在温室聊天时利利会顺手递菜苗)。
- **NPC 主动搭话**:好感 ≥ 阈值的 NPC 与玩家同地点时,按概率主动打一句招呼(SSE 推送 + 头顶气泡),让世界「注意到」玩家。

### C. 交互的后果与回报回路(可玩性核心)

- **玩家进入关系网**:每个 NPC 对玩家有 affinity + impression(对话后由 LLM 顺带更新一句印象,Mock 模式按关键词规则更新);档案面板展示「TA 眼中的你」——玩家行为的镜子。
- **心愿系统(quest-lite,数据驱动不写死)**:NPC 依据当前状态在对话中自然带出小心愿(小柯缺零件、利利想给莫莫捎热汤、莫莫想找一张旧唱片、阿羯要人替他值一晚)。心愿 = `{发起者, 类型: 取物/传话/陪伴, 目标, 回报}`,由后端依状态生成入池,LLM 对话时从池中自然提及;完成 → 好感提升 + 双方日记记一笔 + 事件流广播。
- **口袋与物品**:玩家有 4 格口袋;物品来自场景交互点(巴士站的旧行李、水潭边的螺母)与 NPC 赠予;送礼/交心愿物均从口袋出。
- **传话机制**:玩家可以「替 A 带话给 B」——把 A 的原话带进 B 的对话上下文,B 的反应写进双方记忆并影响两人 affinity。AI 涌现的最佳展示位。
- **小镇手记(收集目标)**:玩家解锁的秘闻碎片(阿羯的「劫」、莫莫的非卖品、利利姐妹往事、小柯的第一件修好的东西)收进「手记」图鉴;高好感对话、完成心愿、特定时间地点触发解锁。给玩家一个「集齐」的长期目标。
- **公告板变玩家的笔**:玩家可在广场公告板**写一句自由文本**,进入全体 NPC 决策与对话上下文,四人依人设各自反应(事件流可见)——原「贴旧照片」按钮的玩家化替代,也是 AI 能力的最佳演示。
- **天气交互 diegetic 化**:改天气从按钮改为水潭边的「蝾螈许愿」互动(掷石祈晴/唤雾,带冷却),包装成世界内行为;观察者模式仍保留直改按钮用于演示。

### D. 工程与降级约束

- 心愿生成、印象更新、传话反应均走 AI Provider 抽象,Mock 模式用模板+规则保证全流程可玩;
- 新增 pytest:心愿生命周期(生成→完成→好感/日记落库)、传话写入双方记忆、玩家存档往返(位置/口袋/手记);
- 新增设定(旧巴士站、废墟界碑、心愿池、玩家身份)同步写回 outline.md,保持单一事实来源。

### 验收标准（v3）

1. 玩家以角色身份在小镇走动,摄像机跟随,靠近 NPC 才能对话;
2. 同一句话分别问四人,回复既符合人设、又引用当下在场情境;
3. 跑通一条完整心愿回路:听到心愿 → 取物/传话 → 交付 → 好感与日记可见变化 + 事件流广播;
4. 在公告板写一句话,能观察到四个 NPC 各自不同的反应;
5. 手记里至少可解锁 2 条秘闻;Mock 模式下 1–4 全部可体验;
6. 原始要求与 v1/v2 能力不回退(观察者模式保留演示入口)。

## v3 Todo

- [ ] A1 玩家精灵 + 点击寻路/WASD + 摄像机跟随,地图扩为 1600×1050（实现与生产构建已完成；当前无可连接浏览器，按键/镜头视觉验收待复核）
- [x] A2 新增地点:旧巴士站(出生/存档点)、废墟界碑;设定写回 outline.md
- [x] A3 玩家状态入世界模型与存档(location/口袋/手记/与各 NPC 关系)
- [ ] B1 邻近对话(距离阈值 + 交互提示 + E 键/点击)（前后端约束与交互提示已实现；浏览器实际按键验收待复核）
- [x] B2 对话 prompt 注入在场情境;NPC 高好感主动搭话
- [x] C1 玩家 affinity/impression 双向关系 + 档案面板「TA 眼中的你」
- [x] C2 心愿池(状态驱动生成/LLM 自然提及/完成结算),Mock 模板兜底
- [x] C3 口袋 4 格 + 场景交互点物品 + 送礼/交付
- [x] C4 传话机制(原话入目标 NPC 上下文,双向记忆与好感)
- [x] C5 小镇手记图鉴 + 秘闻解锁条件
- [x] C6 公告板自由文本 → 全 NPC 上下文与差异化反应
- [x] C7 天气改为水潭「蝾螈许愿」(冷却);观察者模式保留调试栏
- [x] D1 新增 pytest:心愿生命周期 / 传话记忆 / 玩家存档往返
- [x] （顺手修 v2.1 遗留:worldSignature 纳入 action.reason;篝火 alpha 与常驻补间冲突改为只补间 scale）

---

# 迭代方向 v3.1 —— 幻觉治理 + 对话可读性（新增需求）

## 问题诊断（基于 v3 体验与代码审查）

1. **NPC 幻觉严重,捏造不存在的东西**。根因:整个 prompt 体系只有 12 行(world.md 5 行、每角色人设 1 行),**没有事实边界**——LLM 不知道世界里真实存在哪些人/物/地点/事件,被问到只好编;且编造内容会写进记忆,下次对话再被当事实引用,**幻觉滚雪球**。
2. **对话页面不直观**。`DialogueOverlay` 只大字展示最新一条回复,历史记录 `slice(-5,-1)` 与「TA 眼中的你」一起挤在右下小区块,字小、无滚动、看不清来龙去脉。

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在给 Inconnewt 做 v3.1:H 组治理 NPC 幻觉,U 组重做对话可读性。原则:**事实由代码保证,感受由 AI 生成**——凡是「世界里有什么/发生过什么」都必须来自结构化数据,LLM 只负责怎么说、何时说。

### H. 幻觉治理(grounding)

1. **事实卡注入**:每次对话/决策的上下文中,注入一段结构化「此刻事实」(由后端从世界状态拼装,不是 LLM 文本):
   - 小镇居民只有莫莫、小柯、阿羯、利利四人 + 一位外来者(玩家名);
   - 地点全集(7 处)与各自的当前在场者;
   - 玩家口袋物品、进行中的心愿、最近 5 条真实事件(取自事件流结构化数据);
   并加硬性指令:「你只能提及以上事实与你记忆中的内容;镇上没有其他居民、店铺、组织;不存在的东西不要提」。
2. **「不知道」角色化**:与其禁止编造,不如把「面对未知」写进人设——momo.md 等各角色文件补一行:被问到不知道的事时,莫莫会摸着旧物岔开话题;阿羯只说「不知道。」;小柯直接「啥?没听说过!」;利利会说「回头帮你问问,先吃饭」。拒绝编造本身成为人设差异的展示位。
3. **秘闻白名单(最关键)**:「劫」的真相、莫莫的非卖品等悬念内容,**正文预先写死在数据里**(canon 文案,同步进 outline.md),LLM 只决定「这轮要不要透露、用什么口吻引出」,绝不自由发挥真相内容。好感/条件未达时,prompt 明确给出各角色的回避方式。
4. **记忆防污染**:
   - 事件类记忆由后端**模板化生成**(谁/何时/何地/做了什么,全部来自结构化数据),LLM 只追加一句「感想」;
   - 对话记忆入库前做实体校验:提及的人名/地名必须在白名单内,含未知实体的句子降级为「聊了些别的」;
   - 日记摘要 prompt 加约束:「只总结确实发生过的事件,不补全、不推测」。
5. **world.md 扩写为「世界圣经」**:从 outline.md 收敛一版 30–50 行的事实与边界章:有什么、没有什么、谁知道什么、谁永远不说什么;对话温度降至 0.6,决策 0.3。
6. **回归验证**:新增 pytest——构造「问不存在的人/店」的对话用例,断言 Mock 模式回复走「不知道」模板;AI 模式做 3 条人工 checklist 写进 README 演示章节。

### U. 对话可读性重做

1. **对话主体改为可滚动的剧本流**:右侧对话区扩为高列(约 44vw × 76vh),按时间从上往下排:NPC 行带小头像+名字左对齐,玩家行右对齐,系统行居中弱化;正文字号 ≥15px、行高 ≥1.7、浅纸底深字保证对比度;新消息自动滚到底,打字机效果只作用于最新一条。
2. **保留演出感**:立绘、压暗、mood 描边不动;最新一条 NPC 回复在流内用醒目样式(稍大字号+左侧色条),不再单独悬浮一块。
3. **「TA 眼中的你」移出对话框**:归位到档案面板关系页;对话框顶部只留一行紧凑状态(好感值 + 当前情境)。
4. **对话记录 backlog**:头部加「记录」按钮,全屏半透明覆盖展示本次会话完整历史(大字号、可滚动、标注说话人与来源 AI/Mock),Esc/点击关闭——参考 Galgame/明日方舟的 log 惯例。
5. **输入降门槛**:输入框上方给 3–4 个情境话题 chips(「今天有什么计划?」「有什么我能帮上的?」「聊聊这个地方」+ 心愿进行中时的「说说那件事」),点击即发送;chips 由当前情境生成,不写死。

### 验收标准（v3.1）

1. 问任一 NPC 三个不存在的事物(人/店/事件),回复全部以角色化方式承认不知道或回避,无捏造;
2. 连续对话 10 轮后查看其记忆与日记,无未发生事件、无白名单外实体;
3. 秘闻透露的内容与 canon 文案一致(可比对);
4. 对话页一眼可读:剧本流滚动顺畅、字号达标,backlog 可查全量历史;
5. Mock 模式下「不知道」模板、话题 chips、backlog 全部可体验;既有能力不回退。

## v3.1 Todo

- [x] H1 后端拼装「此刻事实」结构化上下文并注入对话/决策 prompt
- [x] H2 各角色 prompt 补「面对未知」的角色化反应
- [x] H3 秘闻 canon 化:文案写死入数据 + outline.md,LLM 只管引出时机与口吻
- [x] H4 记忆防污染:事件模板化生成 + 入库实体校验 + 摘要约束
- [x] H5 world.md 扩写为世界圣经(30–50 行);对话温度 0.6 / 决策 0.3
- [x] H6 pytest:未知实体问答走「不知道」;README 加 AI 模式人工 checklist
- [x] U1 对话主体改可滚动剧本流(字号/行高/对比度达标,打字机只作用最新条)
- [x] U2 「TA 眼中的你」移入档案关系页,对话框顶部改紧凑状态行
- [x] U3 全屏对话记录 backlog(大字号,标注说话人与 AI/Mock 来源)
- [x] U4 情境话题 chips(依当前情境/心愿生成,点击即发送)

---

# 迭代方向 v3.2 —— 对话意图理解:从关键词映射到 agent 自主理解（新增需求）

## 问题诊断

「NPC 听懂玩家的话并采取行动」目前只支持 follow/stop 两种意图:DeepSeek 路径已由 LLM 判断(方向是对的),但意图空间太窄;Mock 路径靠 `_mock_companion_intent` 的关键词短语表,想覆盖「你去帮莫莫看看」「晚点广场找我」这类委托/约定意图只能无限堆词条——组合爆炸,这条路走不通(实现 agent 卡死的根源)。**结论:不要再扩关键词表;把理解完全交给 LLM,代码只负责定义「世界能做什么」并校验。**

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在给 Inconnewt 重做「对话 → NPC 行动」链路。设计原则延续 v3.1:**能力由代码定义(封闭动词表),理解由 AI 完成(结构化输出)**。禁止新增任何关键词→意图的映射代码。

### 1. 行为动词表(封闭、数据驱动)

定义 NPC 可被对话触发的世界行为全集,放数据文件不写死在代码里:

```jsonc
[
  { "verb": "follow_player" },                       // 跟随玩家
  { "verb": "stop_following" },
  { "verb": "goto",  "args": { "location": "地点id" } },   // 去某地
  { "verb": "do",    "args": { "action": "动作id" } },     // 执行地点动作(吃饭/巡逻/修理…)
  { "verb": "visit", "args": { "npc": "npc_id" } },        // 去找某人
  { "verb": "relay", "args": { "npc": "npc_id", "text": "…" } }, // 替玩家转告
  { "verb": "none" }
]
```

### 2. LLM 一次调用同时产出回复与意图

chat 的 JSON schema 从 `companion_action` 扩为通用 `intents` 数组(0–N 个):

```jsonc
{ "reply": "…", "affinity_delta": 1, "impression": "…",
  "intents": [ { "verb": "goto", "args": { "location": "greenhouse" }, "because": "玩家说利利在等我吃饭" } ] }
```

- 上下文中给出动词表全集 + 当前可行条件(地点/在场者/自身状态),由 LLM 判断玩家的话对应哪些意图——**不管玩家怎么措辞**(委托、暗示、反话皆可理解);
- prompt 明确:意图必须是「你这个角色此刻愿意做的事」——低好感的阿羯听到「跟我走」可以在 reply 里拒绝并输出空 intents。**拒绝也是人设展示位**;
- 意图识别不单独发第二次 LLM 调用,合并在 chat 一次输出里(现有 DeepSeek 路径已如此,保持)。

### 3. 后端:校验、入队、不阻塞

- **白名单校验**:verb 必须在动词表内,args 里的地点/NPC/动作 id 必须真实存在,非法即丢弃(静默,不报错给玩家);
- **可行性检查**:按世界状态判断(已在目的地则忽略 goto;夜里熟睡的 NPC 不接受 visit 等),不可行丢弃;
- **执行走 tick 队列**:意图入 NPC 的行动队列,chat 响应**立即返回**,下一 tick 执行并 SSE 广播(reason 带上「因为你刚才说…」)——严禁 chat 请求同步等待 NPC 移动完成(防卡死);
- 执行结果写入 NPC 记忆与事件流,保持 v3.1 的模板化事实生成。

### 4. Mock 模式:承认边界,不堆词条

- Mock 保留现有 follow/stop 短语表作为**最低可体验**,不再扩充;README 明确写出「意图自由理解是真实 AI 能力,Mock 仅支持跟随/停止」——这本身就是「AI 真正被用起来」的最佳对照演示(评估项 4 的加分位);
- 现有 `_mock_companion_intent` 收纳进 Mock Provider,输出统一转成 intents 协议,链路只有一套。

### 5. 测试

- pytest:intents 白名单校验(非法 verb/args 丢弃)、不可行意图丢弃、意图入队后 chat 立即返回(响应时间断言)、Mock follow/stop 回归;
- 人工 checklist(README):「去陪利利吃个饭吧」→ goto+do;「帮我告诉阿羯今晚风大」→ relay;「别跟着我了」→ stop_following;低好感阿羯拒绝同行。

### 验收标准（v3.2）

1. 用五种不同措辞表达「跟我来」「去某地」「捎话」,AI 模式下 NPC 均正确理解并在下一 tick 行动,气泡 reason 引用玩家原话;
2. 无任何新增关键词映射代码;非法/不可行意图静默丢弃,不产生错误行为;
3. chat 响应不因 NPC 行动而阻塞(流式回复即时,行动异步可见);
4. Mock 模式 follow/stop 仍可体验,README 写明两种模式的能力边界。

## v3.2 Todo

- [x] T1 行为动词表数据文件(verb + args schema + 可行性条件)
- [x] T2 chat schema 扩为 intents 数组;prompt 加动词表与「角色意愿过滤」
- [x] T3 后端白名单/可行性校验 + tick 行动队列(chat 立即返回)
- [x] T4 执行结果写记忆/事件流(reason 引用玩家的话)
- [x] T5 Mock 收编进统一 intents 协议(仅 follow/stop),README 写明边界
- [x] T6 pytest:白名单/可行性/非阻塞/Mock 回归 + README 人工 checklist

---

# 迭代方向 v3.3 —— 存档管理:每位访客一个世界（新增需求）

## 问题诊断

当前全服务器只有**一个世界实例**(`world_state` 表写死 `id=1`),没有玩家身份概念:公开地址被第二个人打开时,两人操控同一个化身、互相覆盖状态与存档;新访客看到的是别人玩过的世界。作为「可访问的线上体验地址」交付物,这会直接影响评审体验——每位访客(包括面试官)都应获得一个干净、独立、可回来继续的世界。

## 方案决策

- **否决纯本地存档**:tick 模拟与 AI 决策都在后端,世界状态必须在服务器;浏览器本地只承担「身份凭证 + 导出/导入」。
- **采用:访客隔离的多世界实例(单机存档语义,服务器托管)**——不做共享世界 MMO(超出 MVP 且与「观察一镇人过日子」的玩法相性差)。

## 优化方向 Prompt（可直接交给开发 agent 执行）

> 你在给 Inconnewt 加多访客存档管理。目标:每位访客一个独立世界,回访可继续,闲置不烧 AI 配额,公开地址可被任意人同时体验。

### 1. 访客身份与世界实例

- 首次访问后端签发 `world_id`(UUID),HttpOnly Cookie 存储(30 天滚动续期);前端 localStorage 冗余一份用于 Cookie 丢失后的恢复提示;
- `world_state` 表改为 `world_id TEXT PRIMARY KEY, payload, last_seen_at`;`saves` 表加 `world_id` 列 + `slot`(1–3 手动槽)与 `kind`(auto/manual);SQLite 单文件即可承载,无需换库;
- 新 `world_id` 从初始化种子数据创建全新世界(第 1 天清晨,四人初始状态)。

### 2. 标题画面接管入口

- 开场标题画面(v2 的可选项转正):检测到已有存档 → 「继续旅程 / 重新开始」;无存档 → 「走进新螈镇」;
- 「重新开始」= 废弃旧 world_id 重建(需二次确认);玩家名在开场输入(默认「外来者」),写入世界供 NPC 称呼。

### 3. Tick 调度与成本控制

- Tick 调度器只推进**活跃世界**(`last_seen_at` 距今 < 5 分钟,即页面开着 SSE 心跳的);闲置世界完全冻结,回访时直接从冻结点继续(不补算离线时间,劫后小镇「等你回来才继续过日子」,叙事上自洽);
- 活跃世界数量上限(如 20,`.env` 可配),超限时新访客进入「观察模式」排队提示——防止公开地址被刷爆 AI 配额;
- 每世界每日 AI 调用预算上限,超限自动落 Mock 并在 UI 标注(复用现有降级链路)。

### 4. 存档操作(玩家可见的部分)

- 「保存」= 写入所选手动槽(3 槽,显示各槽时间与游戏内天数);自动存档每 10 tick 一次(1 槽滚动);
- 「恢复」= 从槽读档(覆盖当前世界前二次确认);
- **导出/导入**:设置里可下载当前世界 JSON 存档文件、或上传导入(校验 schema 与版本号)——换浏览器/清 Cookie 后的自救通道,也是「本地存档」诉求的正确落点;
- 世界快照加 `schema_version` 字段,导入与读档时做版本校验,不兼容给明确提示。

### 5. 清理与工程

- 每日定时清理:`last_seen_at` 超过 30 天的世界与其存档删除;
- pytest:两个 world_id 状态互不串扰、槽位往返一致、导出→导入等价、活跃上限触发观察模式、闲置世界不被 tick;
- README 更新:存档机制说明(服务器托管、Cookie 身份、导出自救)、活跃上限与 AI 预算配置项。

### 验收标准（v3.3）

1. 两个浏览器(或隐身窗口)同时访问公开地址,各自拥有独立世界,互不可见、互不干扰;
2. 关闭页面次日回访,「继续旅程」恢复到离开时的状态;
3. 只有开着页面的世界在消耗 tick 与 AI 调用(后端日志可验证);
4. 三个手动槽 + 自动档可用;导出的存档文件在清空 Cookie 后可导入还原;
5. 既有单世界的全部能力(模拟/对话/意图/降级/测试)不回退。

## v3.3 Todo

- [x] S1 world_id 签发(HttpOnly Cookie + 30 天续期)与多世界表结构迁移
- [x] S2 初始化种子 → 新世界工厂;玩家名开场输入
- [x] S3 标题画面:继续旅程 / 重新开始(二次确认)
- [x] S4 Tick 只跑活跃世界(SSE 心跳更新 last_seen_at);活跃上限 + 观察模式
- [x] S5 每世界 AI 日预算 → 超限落 Mock(UI 标注)
- [x] S6 3 手动槽 + 滚动自动档;读档二次确认
- [x] S7 存档导出/导入(schema_version 校验)
- [x] S8 30 天闲置清理任务
- [x] S9 pytest:隔离性/槽位往返/导入等价/活跃上限/闲置冻结;README 更新
